apiVersion: v1
kind: ServiceAccount
metadata:
  name: musclequant-app
  namespace: musclequant
  annotations:
    # IRSA: this ServiceAccount assumes the IAM role that can read the app secret.
    eks.amazonaws.com/role-arn: ${APP_IRSA_ROLE}
automountServiceAccountToken: true
